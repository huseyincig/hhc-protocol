{
  "name": "browser_file_upload",
  "title": "Upload File to Web Page",
  "description": "Uploads client-side files into a file input in the page. Upload sources are validated against the same HHC filesystem policy as file_read: absolute paths only, symlinks resolved, traversal denied, outside-policy reads fail with BROWSER_UPLOAD_DENIED instead of bypassing file policy.",
  "inputSchema": {
    "type": "object",
    "additionalProperties": false,
    "required": [
      "client_id",
      "paths"
    ],
    "properties": {
      "client_id": {
        "type": "string",
        "pattern": "^hhc_[0-9a-f]{64}$",
        "minLength": 68,
        "maxLength": 68,
        "description": "Target HHC client. Absolute routing handle, independent of any connection.",
        "x-mcp-header": "Client-Id"
      },
      "browser_id": {
        "type": "string",
        "pattern": "^brw_[0-9a-f]{32}$",
        "description": "Browser handle from browser_create. Omit to use the ephemeral default context."
      },
      "target": {
        "type": "string",
        "description": "Element reference from a browser_snapshot/browser_find result (e.g. \"e17\"), or a CSS selector for backward compatibility. Refs expire on navigation or DOM change; stale refs fail with BROWSER_STALE_TARGET."
      },
      "paths": {
        "type": "array",
        "items": {
          "type": "string",
          "maxLength": 4096
        },
        "minItems": 1,
        "maxItems": 10,
        "description": "Absolute file paths on the selected client."
      }
    }
  },
  "outputSchema": {
    "type": "object",
    "properties": {
      "status": {
        "type": "string",
        "description": "completed on success, failed on error."
      },
      "client_id": {
        "type": "string",
        "description": "Echo of the target host id (null when no host was targeted)."
      },
      "job_id": {
        "type": "string",
        "description": "Server job handle for this execution."
      },
      "tool": {
        "type": "string",
        "description": "Echo of the invoked tool name."
      },
      "duration_ms": {
        "type": [
          "integer",
          "null"
        ],
        "description": "Execution time in milliseconds."
      },
      "result": {
        "type": "object",
        "properties": {
          "browser_id": {
            "type": [
              "string",
              "null"
            ],
            "description": "Echo of the browser session handle."
          },
          "uploaded": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "path": {
                  "type": "string",
                  "description": "Uploaded source path."
                },
                "filename": {
                  "type": "string",
                  "description": "File name."
                },
                "size_bytes": {
                  "type": "integer",
                  "minimum": 0,
                  "description": "File size in bytes."
                }
              }
            },
            "description": "Uploaded files as {path, filename, size_bytes}."
          }
        },
        "description": "Agent payload for this execution."
      },
      "error": {
        "type": [
          "string",
          "null"
        ],
        "description": "Machine-readable error code when status is failed."
      },
      "retryable": {
        "type": "boolean",
        "description": "Whether a failed call may succeed on retry."
      }
    }
  },
  "annotations": {
    "readOnlyHint": false,
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": true
  }
}
