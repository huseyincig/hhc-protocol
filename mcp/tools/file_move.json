{
  "name": "file_move",
  "title": "Move File",
  "description": "Moves a regular-file or symlink leaf between allowlisted paths on an explicitly selected HHC client after host-policy admission.",
  "inputSchema": {
    "type": "object",
    "additionalProperties": false,
    "required": [
      "client_id",
      "source",
      "destination"
    ],
    "properties": {
      "client_id": {
        "type": "string",
        "pattern": "^hhc_[0-9a-f]{64}$",
        "minLength": 68,
        "maxLength": 68,
        "description": "Target HHC client. Absolute routing handle, independent of any connection.",
        "x-mcp-header": "Client-Id"
      },
      "source": {
        "type": "string",
        "maxLength": 4096,
        "description": "Absolute source path (file or symlink leaf) on the selected client."
      },
      "destination": {
        "type": "string",
        "maxLength": 4096,
        "description": "Absolute destination path on the selected client."
      },
      "overwrite": {
        "type": "boolean",
        "default": false,
        "description": "Replace the destination if it already exists."
      }
    }
  },
  "outputSchema": {
    "type": "object",
    "properties": {
      "status": {
        "type": "string",
        "enum": [
          "completed",
          "failed"
        ],
        "description": "completed on success, failed on error."
      },
      "tool": {
        "type": "string",
        "description": "Echo of the invoked tool name."
      },
      "client_id": {
        "type": "string",
        "description": "Echo of the target host id (null when no host was targeted)."
      },
      "source": {
        "type": "string",
        "description": "Source path that was moved."
      },
      "destination": {
        "type": "string",
        "description": "Destination path of the move."
      },
      "moved": {
        "type": "boolean",
        "description": "Whether the move took effect."
      },
      "error": {
        "type": [
          "string",
          "null"
        ],
        "description": "Machine-readable error code when status is failed."
      },
      "retryable": {
        "type": "boolean",
        "description": "Whether a failed call may succeed on retry."
      }
    }
  },
  "annotations": {
    "readOnlyHint": false,
    "destructiveHint": true,
    "idempotentHint": false,
    "openWorldHint": false
  }
}
