{
  "name": "client_enroll",
  "title": "Enroll Host",
  "description": "Creates a new HHC client identity and one-time visible enrollment script for a supported platform.",
  "inputSchema": {
    "type": "object",
    "additionalProperties": false,
    "required": [
      "platform"
    ],
    "properties": {
      "platform": {
        "type": "string",
        "enum": [
          "any",
          "windows",
          "linux",
          "macos"
        ],
        "description": "Target platform for the enrollment instructions."
      },
      "name": {
        "type": "string",
        "maxLength": 128,
        "description": "Display name for the new host."
      },
      "server_url": {
        "type": "string",
        "maxLength": 2048,
        "description": "Hub URL the new host will connect to."
      }
    }
  },
  "outputSchema": {
    "type": "object",
    "properties": {
      "status": {
        "type": "string",
        "enum": [
          "completed",
          "failed"
        ],
        "description": "completed on success, failed on error."
      },
      "tool": {
        "type": "string",
        "description": "Echo of the invoked tool name."
      },
      "enrollment_token": {
        "type": "string",
        "description": "One-time enrollment token."
      },
      "host_id": {
        "type": "string",
        "description": "New host identity."
      },
      "expires_at": {
        "type": "string",
        "description": "Token expiry timestamp."
      },
      "install_commands": {
        "type": "object",
        "properties": {
          "linux": {
            "type": "string"
          },
          "windows": {
            "type": "string"
          },
          "macos": {
            "type": "string"
          }
        },
        "description": "Per-platform install commands."
      },
      "error": {
        "type": "string",
        "description": "Machine-readable error code when status is failed."
      },
      "retryable": {
        "type": "boolean",
        "description": "Whether a failed call may succeed on retry."
      }
    }
  },
  "annotations": {
    "readOnlyHint": false,
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false
  }
}
