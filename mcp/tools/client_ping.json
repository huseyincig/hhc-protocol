{
  "name": "client_ping",
  "title": "Ping Host",
  "description": "Checks whether the selected HHC client is reachable through the hub and reports session freshness. Read-only; never executes anything on the host. Use for connectivity checks, not full system inspection; prefer system_snapshot when host status is needed.",
  "inputSchema": {
    "type": "object",
    "additionalProperties": false,
    "required": [
      "client_id"
    ],
    "properties": {
      "client_id": {
        "type": "string",
        "pattern": "^hhc_[0-9a-f]{64}$",
        "minLength": 68,
        "maxLength": 68,
        "description": "Target HHC client. Absolute routing handle, independent of any connection.",
        "x-mcp-header": "Client-Id"
      }
    }
  },
  "outputSchema": {
    "type": "object",
    "properties": {
      "status": {
        "type": "string"
      },
      "client_id": {
        "type": "string"
      },
      "reachable": {
        "type": "boolean"
      },
      "online": {
        "type": "boolean"
      },
      "last_seen_age_ms": {
        "type": [
          "integer",
          "null"
        ]
      },
      "error": {
        "type": "string"
      }
    }
  },
  "annotations": {
    "readOnlyHint": true,
    "destructiveHint": false,
    "idempotentHint": true,
    "openWorldHint": false
  }
}
