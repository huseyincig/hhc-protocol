{
  "name": "log_follow_start",
  "title": "Start Log Follow",
  "description": "Opens a stateful tail handle on a client log file; read incrementally with log_follow_read. Prefer over repeated log_read polling.",
  "inputSchema": {
    "type": "object",
    "additionalProperties": false,
    "required": [
      "client_id",
      "source"
    ],
    "properties": {
      "client_id": {
        "type": "string",
        "pattern": "^hhc_[0-9a-f]{64}$",
        "minLength": 68,
        "maxLength": 68,
        "description": "Target HHC client. Absolute routing handle, independent of any connection.",
        "x-mcp-header": "Client-Id"
      },
      "source": {
        "type": "string",
        "enum": [
          "client",
          "mcp-server",
          "mcp-tunnel"
        ],
        "description": "Named log source. Ignored when path is given."
      },
      "path": {
        "type": "string",
        "description": "Explicit log file path (optional).",
        "maxLength": 4096
      },
      "max_lines": {
        "type": "integer",
        "minimum": 1,
        "maximum": 1000,
        "default": 200,
        "description": "Maximum backlog lines to return with the new handle."
      }
    }
  },
  "outputSchema": {
    "type": "object",
    "properties": {
      "status": {
        "type": "string",
        "enum": [
          "completed",
          "failed"
        ],
        "description": "completed on success, failed on error."
      },
      "tool": {
        "type": "string",
        "description": "Echo of the invoked tool name."
      },
      "client_id": {
        "type": "string",
        "description": "Echo of the target host id."
      },
      "handle": {
        "type": "string",
        "description": "Opaque log handle."
      },
      "content": {
        "type": "string",
        "description": "Initial bounded log snapshot text."
      },
      "cursor": {
        "type": "integer",
        "description": "Resume offset for log_follow_read."
      },
      "eof": {
        "type": "boolean",
        "description": "Whether the read reached end of file."
      },
      "error": {
        "type": [
          "string",
          "null"
        ],
        "description": "Machine-readable error code when status is failed."
      },
      "retryable": {
        "type": "boolean",
        "description": "Whether a failed call may succeed on retry."
      }
    }
  },
  "annotations": {
    "readOnlyHint": false,
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false
  }
}
