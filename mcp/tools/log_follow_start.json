{
  "name": "log_follow_start",
  "title": "Start Log Follow",
  "description": "Opens a stateful tail handle on a client log file; read incrementally with log_follow_read. Prefer over repeated log_read polling.",
  "inputSchema": {
    "type": "object",
    "additionalProperties": false,
    "required": [
      "client_id",
      "source"
    ],
    "properties": {
      "client_id": {
        "type": "string",
        "pattern": "^hhc_[0-9a-f]{64}$",
        "minLength": 68,
        "maxLength": 68,
        "description": "Target HHC client. Absolute routing handle, independent of any connection.",
        "x-mcp-header": "Client-Id"
      },
      "source": {
        "type": "string",
        "enum": [
          "client",
          "mcp-server",
          "mcp-tunnel"
        ],
        "description": "Named log source. Ignored when path is given."
      },
      "path": {
        "type": "string",
        "description": "Explicit log file path (optional).",
        "maxLength": 4096
      },
      "max_lines": {
        "type": "integer",
        "minimum": 1,
        "maximum": 1000,
        "default": 200,
        "description": "Maximum backlog lines to return with the new handle."
      }
    }
  },
  "outputSchema": {
    "type": "object",
    "properties": {
      "status": {
        "type": "string",
        "enum": [
          "completed",
          "failed"
        ],
        "description": "completed on success, failed on error."
      },
      "client_id": {
        "type": "string",
        "description": "Echo of the target host id (null when no host was targeted)."
      },
      "job_id": {
        "type": "string",
        "description": "Server job handle for this execution."
      },
      "tool": {
        "type": "string",
        "description": "Echo of the invoked tool name."
      },
      "duration_ms": {
        "type": [
          "integer",
          "null"
        ],
        "description": "Execution time in milliseconds."
      },
      "result": {
        "type": "object",
        "properties": {
          "handle": {
            "type": "string",
            "description": "New log-follow handle."
          },
          "content": {
            "type": "string",
            "description": "Backlog window text."
          },
          "cursor": {
            "type": "integer",
            "description": "Resume offset for the next call."
          },
          "eof": {
            "type": "boolean",
            "description": "Whether the cursor reached end of file."
          }
        },
        "description": "Agent payload for this execution."
      },
      "error": {
        "type": [
          "string",
          "null"
        ],
        "description": "Machine-readable error code when status is failed."
      },
      "retryable": {
        "type": "boolean",
        "description": "Whether a failed call may succeed on retry."
      }
    }
  },
  "annotations": {
    "readOnlyHint": false,
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false
  }
}
