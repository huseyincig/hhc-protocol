{
  "name": "gui_launch",
  "title": "Launch Application",
  "description": "Launches an allowlisted GUI application or developer editor in the active interactive user session through the HHC broker. Use gui_launch for browser/desktop GUI intent; do not use shell_exec, cmd/start, or Session 0 workarounds for interactive GUI. Use HHC browser tools instead when the goal is automated web interaction.",
  "inputSchema": {
    "type": "object",
    "additionalProperties": false,
    "required": [
      "client_id",
      "application"
    ],
    "properties": {
      "client_id": {
        "type": "string",
        "pattern": "^hhc_[0-9a-f]{64}$",
        "minLength": 68,
        "maxLength": 68,
        "description": "Target HHC client. Absolute routing handle, independent of any connection.",
        "x-mcp-header": "Client-Id"
      },
      "application": {
        "type": "string",
        "enum": [
          "default_browser",
          "edge",
          "chrome",
          "explorer",
          "notepad",
          "code",
          "cursor",
          "terminal"
        ],
        "description": "Application to launch in the user session (allowlisted)."
      },
      "target": {
        "type": "string",
        "maxLength": 4096,
        "description": "Optional document, URL, or path opened by the application."
      },
      "username": {
        "type": "string",
        "maxLength": 256,
        "description": "Target interactive username; omit to use the active console session."
      }
    }
  },
  "outputSchema": {
    "type": "object",
    "properties": {
      "status": {
        "type": "string",
        "enum": [
          "completed",
          "failed"
        ],
        "description": "completed on success, failed on error."
      },
      "client_id": {
        "type": "string",
        "description": "Echo of the target host id (null when no host was targeted)."
      },
      "job_id": {
        "type": "string",
        "description": "Server job handle for this execution."
      },
      "tool": {
        "type": "string",
        "description": "Echo of the invoked tool name."
      },
      "duration_ms": {
        "type": [
          "integer",
          "null"
        ],
        "description": "Execution time in milliseconds."
      },
      "result": {
        "type": "object",
        "properties": {
          "application": {
            "type": "string",
            "description": "Launched application."
          },
          "target": {
            "type": "string",
            "description": "Launch target."
          },
          "user": {
            "type": "string",
            "description": "Session user."
          },
          "session_id": {
            "type": "string",
            "description": "Broker session id."
          },
          "pid": {
            "type": [
              "integer",
              "null"
            ],
            "description": "Operating-system PID (null while unknown)."
          }
        },
        "description": "Agent payload for this execution."
      },
      "error": {
        "type": [
          "string",
          "null"
        ],
        "description": "Machine-readable error code when status is failed."
      },
      "retryable": {
        "type": "boolean",
        "description": "Whether a failed call may succeed on retry."
      }
    }
  },
  "annotations": {
    "readOnlyHint": false,
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false
  }
}
