{
  "name": "file_delete",
  "title": "Delete File or Directory",
  "description": "Deletes an allowlisted file/symlink leaf or explicitly recursive directory on an explicitly selected HHC client after destructive host-policy admission.",
  "inputSchema": {
    "type": "object",
    "additionalProperties": false,
    "required": [
      "client_id",
      "path"
    ],
    "properties": {
      "client_id": {
        "type": "string",
        "pattern": "^hhc_[0-9a-f]{64}$",
        "minLength": 68,
        "maxLength": 68,
        "description": "Target HHC client. Absolute routing handle, independent of any connection.",
        "x-mcp-header": "Client-Id"
      },
      "path": {
        "type": "string",
        "maxLength": 4096,
        "description": "Absolute path of the file, symlink leaf, or directory to delete on the selected client."
      },
      "recursive": {
        "type": "boolean",
        "default": false,
        "description": "Also delete directory contents; required for a non-empty directory."
      }
    }
  },
  "outputSchema": {
    "type": "object",
    "properties": {
      "status": {
        "type": "string",
        "enum": [
          "completed",
          "failed"
        ],
        "description": "completed on success, failed on error."
      },
      "tool": {
        "type": "string",
        "description": "Echo of the invoked tool name."
      },
      "client_id": {
        "type": "string",
        "description": "Echo of the target host id."
      },
      "job_id": {
        "type": "string",
        "description": "Server job handle for this execution."
      },
      "duration_ms": {
        "type": [
          "integer",
          "null"
        ],
        "description": "Execution time in milliseconds."
      },
      "result": {
        "type": "object",
        "properties": {
          "path": {
            "type": "string",
            "description": "Deleted path."
          },
          "deleted": {
            "type": "boolean",
            "description": "Whether the entry was deleted."
          },
          "recursive": {
            "type": "boolean",
            "description": "Whether a directory tree was removed."
          },
          "type": {
            "type": "string",
            "enum": [
              "file",
              "directory",
              "symlink"
            ],
            "description": "Kind of entry that was deleted."
          }
        },
        "description": "Agent payload for this execution."
      },
      "error": {
        "type": [
          "string",
          "null"
        ],
        "description": "Machine-readable error code when status is failed."
      },
      "retryable": {
        "type": "boolean",
        "description": "Whether a failed call may succeed on retry."
      }
    }
  },
  "annotations": {
    "readOnlyHint": false,
    "destructiveHint": true,
    "idempotentHint": true,
    "openWorldHint": false
  }
}
