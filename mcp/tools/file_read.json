{
  "name": "file_read",
  "title": "Read File",
  "description": "Reads a bounded file from an allowlisted client path. Prefer this over shell commands such as cat, type, or Get-Content.",
  "inputSchema": {
    "type": "object",
    "additionalProperties": false,
    "required": [
      "client_id",
      "path"
    ],
    "properties": {
      "client_id": {
        "type": "string",
        "pattern": "^hhc_[0-9a-f]{64}$",
        "minLength": 68,
        "maxLength": 68,
        "description": "Target HHC client. Absolute routing handle, independent of any connection.",
        "x-mcp-header": "Client-Id"
      },
      "path": {
        "type": "string",
        "maxLength": 4096,
        "description": "Absolute path of the file to read on the selected client."
      },
      "max_bytes": {
        "type": "integer",
        "minimum": 1,
        "maximum": 65536,
        "default": 65536,
        "description": "Maximum bytes to return; larger output is truncated."
      }
    }
  },
  "outputSchema": {
    "type": "object",
    "properties": {
      "status": {
        "type": "string",
        "description": "completed on success, failed on error."
      },
      "client_id": {
        "type": "string",
        "description": "Echo of the target host id (null when no host was targeted)."
      },
      "job_id": {
        "type": "string",
        "description": "Server job handle for this execution."
      },
      "tool": {
        "type": "string",
        "description": "Echo of the invoked tool name."
      },
      "duration_ms": {
        "type": [
          "integer",
          "null"
        ],
        "description": "Execution time in milliseconds."
      },
      "result": {
        "type": "object",
        "properties": {
          "path": {
            "type": "string",
            "description": "Resolved absolute path that was read."
          },
          "content": {
            "type": "string",
            "description": "File content read (bounded by max_bytes)."
          },
          "bytes": {
            "type": "integer",
            "minimum": 0,
            "description": "Total file size in bytes."
          },
          "truncated": {
            "type": "boolean",
            "description": "Whether content was cut at max_bytes."
          }
        },
        "description": "Agent payload for this execution."
      },
      "error": {
        "type": "string",
        "description": "Machine-readable error code when status is failed."
      },
      "retryable": {
        "type": "boolean",
        "description": "Whether a failed call may succeed on retry."
      }
    }
  },
  "annotations": {
    "readOnlyHint": true,
    "destructiveHint": false,
    "idempotentHint": true,
    "openWorldHint": false
  }
}
