{
  "name": "privileged_shell_exec",
  "title": "Run Admin Command",
  "description": "Runs an elevated root/administrator command on a supported HHC client via the cryptographic privileged helper daemon. client_id and command are mandatory. Highest-risk operation: grants root/administrator execution. Explicit Admin authorization applies and user confirmation is recommended before execution.",
  "inputSchema": {
    "type": "object",
    "additionalProperties": false,
    "required": [
      "client_id",
      "command"
    ],
    "properties": {
      "client_id": {
        "type": "string",
        "pattern": "^hhc_[0-9a-f]{64}$",
        "minLength": 68,
        "maxLength": 68,
        "description": "Target HHC client. Absolute routing handle, independent of any connection.",
        "x-mcp-header": "Client-Id"
      },
      "command": {
        "type": "string",
        "description": "Shell command line executed with root/Administrator privileges.",
        "minLength": 1
      },
      "timeout_seconds": {
        "type": "integer",
        "minimum": 1,
        "maximum": 300,
        "default": 30,
        "description": "Wall-clock bound in seconds for the command."
      }
    }
  },
  "outputSchema": {
    "type": "object",
    "properties": {
      "status": {
        "type": "string",
        "enum": [
          "completed",
          "failed"
        ],
        "description": "completed on success, failed on error."
      },
      "tool": {
        "type": "string",
        "description": "Echo of the invoked tool name."
      },
      "client_id": {
        "type": "string",
        "description": "Echo of the target host id (null when no host was targeted)."
      },
      "exit_code": {
        "type": "integer",
        "description": "Exit code (null while running)."
      },
      "stdout": {
        "type": "string",
        "description": "Captured standard output text."
      },
      "stderr": {
        "type": "string",
        "description": "Captured standard error text."
      },
      "duration_ms": {
        "type": "integer",
        "description": "Operation duration in milliseconds."
      },
      "elevation_tier": {
        "type": "string",
        "enum": [
          "root",
          "administrator"
        ],
        "description": "Privilege tier granted for the execution."
      },
      "error": {
        "type": "string",
        "description": "Machine-readable error code when status is failed."
      },
      "retryable": {
        "type": "boolean",
        "description": "Whether a failed call may succeed on retry."
      }
    }
  },
  "annotations": {
    "readOnlyHint": false,
    "destructiveHint": true,
    "idempotentHint": false,
    "openWorldHint": false
  }
}
