{
  "name": "browser_close",
  "title": "Close Browser Session",
  "description": "Closes a browser session created by browser_create and releases its cookies, storage, pages and downloads metadata. Idempotent: closing an already-closed or expired handle succeeds with existed=false. Ephemeral sessions leave no persistent state behind.",
  "inputSchema": {
    "type": "object",
    "additionalProperties": false,
    "required": [
      "client_id",
      "browser_id"
    ],
    "properties": {
      "client_id": {
        "type": "string",
        "pattern": "^hhc_[0-9a-f]{64}$",
        "minLength": 68,
        "maxLength": 68,
        "description": "Target HHC client. Absolute routing handle, independent of any connection.",
        "x-mcp-header": "Client-Id"
      },
      "browser_id": {
        "type": "string",
        "pattern": "^brw_[0-9a-f]{32}$",
        "description": "Opaque browser handle returned by browser_create. 128-bit entropy, never parsed."
      }
    }
  },
  "outputSchema": {
    "type": "object",
    "properties": {
      "status": {
        "type": "string",
        "description": "completed on success, failed on error."
      },
      "browser_id": {
        "type": "string",
        "description": "Echo of the browser session handle."
      },
      "existed": {
        "type": "boolean",
        "description": "Whether the session existed before this call."
      },
      "error": {
        "type": [
          "string",
          "null"
        ],
        "description": "Machine-readable error code when status is failed."
      },
      "retryable": {
        "type": "boolean",
        "description": "Whether a failed call may succeed on retry."
      }
    }
  },
  "annotations": {
    "readOnlyHint": false,
    "destructiveHint": false,
    "idempotentHint": true,
    "openWorldHint": false
  }
}
