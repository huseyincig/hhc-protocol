{
  "name": "log_follow_stop",
  "title": "Stop Log Follow",
  "description": "Closes a log-follow handle and releases its state.",
  "inputSchema": {
    "type": "object",
    "additionalProperties": false,
    "required": [
      "handle"
    ],
    "properties": {
      "handle": {
        "type": "string",
        "pattern": "^log_[0-9a-f]{32}$",
        "description": "Opaque log-follow handle returned by log_follow_start."
      }
    }
  },
  "outputSchema": {
    "type": "object",
    "properties": {
      "status": {
        "type": "string",
        "description": "completed on success, failed on error.",
        "enum": [
          "completed",
          "failed"
        ]
      },
      "error": {
        "type": [
          "string",
          "null"
        ],
        "description": "Machine-readable error code when status is failed."
      },
      "retryable": {
        "type": "boolean",
        "description": "Whether a failed call may succeed on retry."
      }
    }
  },
  "annotations": {
    "readOnlyHint": false,
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false
  }
}
