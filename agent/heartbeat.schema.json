{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "HHC heartbeat / ping-pong",
  "type": "object",
  "properties": {
    "type": {
      "enum": [
        "ping",
        "pong",
        "heartbeat"
      ]
    },
    "timestamp": {
      "type": "string",
      "format": "date-time"
    }
  },
  "required": [
    "type"
  ],
  "additionalProperties": true
}
